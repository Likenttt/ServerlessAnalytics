{
  "name": "@serverless-analytics/cloudflare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@serverless-analytics/core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260924.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.138.0"
  }
}
